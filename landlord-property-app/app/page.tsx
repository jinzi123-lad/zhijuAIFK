import { RoleSelection } from "@/components/role-selection"

export default function HomePage() {
  return (
    <main className="mesh-gradient min-h-screen relative flex items-center justify-center">
      <RoleSelection />
    </main>
  )
}
